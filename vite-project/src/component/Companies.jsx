

export default function Companies(props) {
  return (
    <div>
           
        <p>Company name : {props.CompanyName}</p>
        <p>Number of employees: {props.number}</p>
        <p>year of incorporation: {props.year}</p>
    </div>
  )
}
